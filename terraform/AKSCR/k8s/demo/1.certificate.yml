apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: certificate
spec:
  secretName: www-tls
  issuerRef:
    name: cert-manager-global
    kind: ClusterIssuer
  dnsNames:
  - www.mapineda48.xyz